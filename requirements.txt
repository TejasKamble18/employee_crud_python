# --- Core Django & REST Framework ---
Django>=5.1
djangorestframework>=3.15
django-cors-headers>=4.4
django-filter>=24.3

# --- Database Drivers ---
mysqlclient>=2.2
dj-database-url>=2.2

# --- Production Server & Static Files ---
gunicorn>=21.2
whitenoise>=6.7

# --- JWT Authentication ---
djangorestframework-simplejwt>=5.3

# --- Utility Libraries ---
python-dotenv>=1.0
requests>=2.32
urllib3>=1.26,<3

# --- Optional (only for local testing or debugging) ---
pytest>=8.3
pytest-django>=4.9
flake8>=7.1

# --- (Optional) AI / Extra API packages you may actually use later ---
openai>=1.0
pandas>=2.2
numpy>=2.2
beautifulsoup4>=4.13

# --- OS-specific (ignored on Render) ---
pywin32==310; sys_platform == 'win32'
pypiwin32==223; sys_platform == 'win32'
comtypes==1.4.11; sys_platform == 'win32'
